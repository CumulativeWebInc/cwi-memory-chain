# CWI Memory Chain

Public attestation records for the CWI agent memory blockchain — hashes and
signatures only. Encrypted memory bodies stay in the private cwi-agent-memory
repo and never appear here.

The iOS-installable verifier app lives in docs/ (served via GitHub Pages):
open it in Safari, tap Share, Add to Home Screen.

Records are mirrored automatically by the anchor CLI after every attestation.
