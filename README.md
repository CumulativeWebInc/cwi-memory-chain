# CWI Memory Chain

Public attestation records for the CWI agent memory blockchain — hashes and signatures only. Encrypted memory bodies stay in the private  repo.

The iOS-installable verifier app lives in  (served via GitHub Pages).
